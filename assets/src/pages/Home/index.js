// @flow

import * as React from 'react';
import { Carousel } from 'antd';

import ContentCard from 'components/ContentCard';
import styles from './index.css';

const imageUrls = [
  '/static/images/screenshot/summary.png',
  '/static/images/screenshot/page-views.jpg',
  '/static/images/screenshot/user-views.jpg',
  '/static/images/screenshot/bandwidth.jpg',
  '/static/images/screenshot/request.jpg',
  '/static/images/screenshot/response.jpg',
  '/static/images/screenshot/user-agent.jpg',
  '/static/images/screenshot/referrer.jpg',
];

class Home extends React.Component<{}> {
  render() {
    return (
      <ContentCard title={null}>
        <h1 className={styles.h1}>Log Analyzer</h1>
        <p className={styles.p}>A web-request-log analyzer, used for analyzing log files generated by web servers.</p>
        <div className={styles.divider} />

        <h2 className={styles.h2}>What can <b>Log Analyzer</b> do for you</h2>
        <p className={styles.p}>
          It will generate a report for each given log file, which offers you the detailed information
          about <b>page views</b>, <b>user views</b>, <b>bandwidth</b>, <b>response time</b>, and information
          about user agent like <b>browsers</b> and <b>operating systems</b> and more.
        </p>
        {/*<Carousel>*/}
          {/*{*/}
            {/*imageUrls.map(url => (*/}
              {/*<div key={url} className={styles.imageWrapper}>*/}
                {/*<img width="100%" src={url} />*/}
              {/*</div>*/}
            {/*))*/}
          {/*}*/}
        {/*</Carousel>*/}
        <div className={styles.divider} />

        <h2 className={styles.h2}>How to use <b>Log Analyzer</b></h2>
      </ContentCard>
    );
  }
}

export default Home;
